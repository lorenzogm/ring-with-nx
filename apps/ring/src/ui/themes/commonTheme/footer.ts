import { RingThemeOptions } from '@ring/core/index'

import { colors } from './colors'

export const footer: RingThemeOptions['footer'] = {
  backgroundColor: colors('teal-4'),
  color: colors('grey'),
}
